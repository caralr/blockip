Linux VPS一键屏蔽指定国家所有的IP访问
本脚本适用于CentOS、Debian、Ubuntu等常用系统。

使用root运行以下命令：

git clone https://github.com/caralr/blockip
chmod +x block.sh
./block.sh

封禁ip时会要求你输入国家代码，代码查看：点击进入。记住所需输入的参数均为小写字母。比如JAPAN (JP)，我们就使用jp这个参数。
